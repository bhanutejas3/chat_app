const host = "http://localhost:6001";
const registerRoute = host + "/api/auth/register";
const loginRoute = host + "/api/auth/login";
const authRoute = host + "/api/auth";

export default host;
export { registerRoute, loginRoute, authRoute };
